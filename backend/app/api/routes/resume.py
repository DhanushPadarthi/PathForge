# Resume processing routes
# POST /api/resume/upload - Upload resume file
# POST /api/resume/parse - Parse uploaded resume
# GET /api/resume/{user_id} - Get user's resume data
# DELETE /api/resume/{user_id} - Delete user's resume
